import { StripfilenamePipe } from './stripfilename.pipe';

describe('StripfilenamePipe', () => {
  it('create an instance', () => {
    const pipe = new StripfilenamePipe();
    expect(pipe).toBeTruthy();
  });
});
